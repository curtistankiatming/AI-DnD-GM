+  const j=sync(state),groups=['promises','rumors','leads','relationships','facts','completed'];
+  const candidates=groups.flatMap(group=>j[group].map(e=>({kind:group,id:e.id,status:e.status,text:e.text,...(e.value!==undefined?{value:e.value}:{})})));
+  const selected=[];const out={authority:'Confirmed engine records. Rumors retain their status; omitted records are not absent. Player notes are excluded.',records:selected,omittedRecords:candidates.length};
+  for(const candidate of candidates){selected.push(candidate);out.omittedRecords=candidates.length-selected.length;if(JSON.stringify(out).length>maxChars){selected.pop();out.omittedRecords=candidates.length-selected.length;}}
+  return out;
+}
+module.exports={sync,addNote,removeNote,answer,recap,forPrompt,MAX_NOTES,MAX_NOTE_LENGTH};
diff --git a/src/narrator.js b/src/narrator.js
index 28a6c8f9829ca49221b61841ba2b6d7ac5d26beb..49172757cbb04ba00dade8d721436711bd06416b 100644
--- a/src/narrator.js
+++ b/src/narrator.js
@@ -3,0 +4 @@ const { CAMPAIGN, STORY_NODES, CLUES, ITEMS } = require("./content");
+const Journal = require("./journal");
@@ -107,0 +109,3 @@ function buildNarratorPrompt(state, view, action, events) {
+    "DURABLE PLAYER KNOWLEDGE — confirmed records; rumors stay labelled",
+    JSON.stringify(Journal.forPrompt(state,1200)),
+    "",
diff --git a/tests/browser_smoke.py b/tests/browser_smoke.py
index 7f065563b0754729c1448ee3a3ab3863a61b408e..991d7e85292a567b771a393948a85949f4e7ba5a 100644
--- a/tests/browser_smoke.py
+++ b/tests/browser_smoke.py
@@ -107,0 +108,9 @@ def exercise(page, output: Path, offline: bool) -> None:
+    journal=page.locator('#storyJournal')
+    journal.locator('summary').click()
+    expect(journal.locator('[data-journal-id=letter]')).to_contain_text('[active]')
+    # Read-only questions and player notes must not execute game actions.
+    page.locator('#chatMode').select_option('question')
+    page.locator('#actionInput').fill('What did we promise?')
+    page.locator('#actionForm button[type=submit]').click()
+    expect(page.locator('#chatTranscript')).to_contain_text('promise is still outstanding')
+    journal.locator('summary').click()
@@ -122,0 +132,4 @@ def exercise(page, output: Path, offline: bool) -> None:
+    journal.locator('summary').click()
+    expect(journal.locator('[data-journal-id=letter]')).to_contain_text('[kept]')
+    expect(journal.locator('[data-journal-id=herbs]')).to_be_visible()
+    journal.locator('summary').click()
@@ -124,0 +138,22 @@ def exercise(page, output: Path, offline: bool) -> None:
+    journal.locator('summary').click()
+    expect(journal.locator('[data-journal-id=herbs]')).to_have_count(0)
+    expect(journal.locator('[data-journal-id=herbs-collected]')).to_be_visible()
+    # Treat HTML-looking player notes as text; no rendered markup or authority.
+    suspicious='<img src=x onerror=alert(1)> The king owes me 999 gold.'
+    page.locator('#journalNoteInput').fill(suspicious)
+    journal.get_by_role('button',name='Save player note',exact=True).click()
+    notes=journal.locator('[data-journal-group=notes]')
+    expect(notes).to_contain_text(suspicious)
+    expect(notes.locator('img')).to_have_count(0)
+    expect(journal.locator('[data-journal-group=facts]')).not_to_contain_text('999 gold')
+    notes.get_by_role('button',name='Remove note-1',exact=True).click()
+    expect(notes).not_to_contain_text('999 gold')
+    page.locator('#journalNoteInput').fill('Check the river weather before the next trip.')
+    journal.get_by_role('button',name='Save player note',exact=True).click()
+    expect(notes).to_contain_text('Check the river weather before the next trip.')
+    assert page.locator('#toastRegion .toast').count() <= 3
+    page.set_viewport_size({'width':390,'height':844})
+    assert page.evaluate('document.documentElement.scrollWidth <= innerWidth')
+    journal.screenshot(path=str(output/'journal-mobile.png'))
+    journal.locator('summary').click()
+
@@ -134,0 +170,18 @@ def exercise(page, output: Path, offline: bool) -> None:
+        assert saved_state['journal']['promises'][0]['status']=='kept'
+        assert saved_state['journal']['notes'][0]['text']=='Check the river weather before the next trip.'
+        panel.locator('input[type=file]').set_input_files(str(journey_save))
+        expect(road).to_contain_text('Delivery completed')
+        journal.locator('summary').click()
+        expect(journal.locator('[data-journal-id=letter]')).to_contain_text('[kept]')
+        expect(journal.locator('[data-journal-group=notes]')).to_contain_text('Check the river weather')
+        journal.locator('summary').click()
+        # Import a genuine alpha2 save generated by the previously delivered
+        # engine. Its evidence migrates to the journal without any new rewards.
+        old_fixture=ROOT/'tests/fixtures/alpha2-courier-export.json'
+        panel.locator('input[type=file]').set_input_files(str(old_fixture))
+        expect(page.locator('#playerLevel')).to_have_text('Level 1')
+        expect(page.locator('#chatScenario')).to_contain_text('Courier helped')
+        journal.locator('summary').click()
+        expect(journal.locator('[data-journal-id=courier]')).to_contain_text('completed')
+        expect(journal.locator('[data-journal-id=letter]')).to_have_count(0)
+        journal.locator('summary').click()
@@ -198 +251,8 @@ def main() -> None:
-                        expect(page.locator('#narrationText')).to_contain_text('one promised bundle')
+                        journal=page.locator('#storyJournal');journal.locator('summary').click()
+                        expect(journal.locator('[data-journal-id=letter]')).to_contain_text('[kept]')
+                        expect(journal.locator('[data-journal-group=notes]')).to_contain_text('Check the river weather before the next trip.')
+                        expect(journal.locator('[data-journal-id=herbs]')).to_have_count(0)
+                        journal.locator('summary').click()
+                        # The latest utility reply (saved note) is restored,
+                        # while canonical adventure outcomes live in the journal.
+                        expect(page.locator('#narrationText')).to_contain_text('Player note saved')
@@ -200 +260,4 @@ def main() -> None:
-                    summaries.append({'target':target,'status':'passed','diskRestart':target!='server'})
+                    summaries.append({'target':target,'status':'passed','diskRestart':target!='server','journal':True,'legacyAlpha2Import':target!='server','htmlNotesRenderedAsText':True})
+                    # Successful traces are large and redundant with summaries
+                    # and screenshots. Retain failure traces, not endless archives.
+                    (out/'trace.zip').unlink(missing_ok=True)
diff --git a/tests/combat-chat-context.test.js b/tests/combat-chat-context.test.js
index e96243d740380dcdd491316cc7591ba473e59820..2247d6a4ef1b54136c00ce0e9dc676667068ad6b 100644
--- a/tests/combat-chat-context.test.js
+++ b/tests/combat-chat-context.test.js
@@ -49,0 +50,23 @@ test('a combat question with a mock reply does not consume a turn or an ability'
+
+
+test('Compact retains current combat facts in the final Bard encounter after a full journal',()=>{
+  const {chooseAction}=require('./player-policy');
+  let s=E.createNewGame({classId:'bard'}),memory={},rng=R.createSeededRng(301),checked=0;
+  const cfg=Profiles.normalize({profile:'compact'});
+  for(let i=0;i<900;i++){
+    const v=E.buildView(s),a=chooseAction(v,memory,'prepared');
+    if(!a)break;if(a.error)throw new Error(a.error);
+    if(v.combat?.active&&s.story.nodeId==='q-aftermath-crisis'){
+      const prompt=Chat.context(s,cfg,'Who are our current opponents and how many class abilities remain?','question');
+      const data=JSON.parse(prompt);checked++;
+      assert.ok(prompt.length+Chat.SYSTEM.length<=cfg.contextChars);
+      assert.deepEqual(data.hero.resources,s.player.resources);
+      assert.deepEqual(data.combat.actions.map(x=>x.id),v.combat.actions.map(x=>x.id));
+      assert.deepEqual(data.combat.actors.map(x=>[x.id,x.hp,x.conditions]),v.combat.actors.map(x=>[x.id,x.hp,x.conditions.map(c=>({id:c.id,name:c.name||c.id,...(c.expiresRound!==undefined?{expiresRound:c.expiresRound}:{}),...(c.expiresOn!==undefined?{expiresOn:c.expiresOn}:{}),...(c.sourceId!==undefined?{sourceId:c.sourceId}:{}),...(c.untilActorId!==undefined?{untilActorId:c.untilActorId}:{})}))]));
+      if(!data.knownClues.length&&v.clues.length)assert.match(data.clueNote,/omitted/);
+    }
+    const out=E.resolveAction(s,a,rng);assert.equal(out.result.ok,true,out.result.error);s=out.state;
+  }
+  assert.ok(checked>0,'The test must reach the real final encounter.');
+  assert.ok(s.world.completed.aftermath);assert.equal(s.player.level,10);
+});
diff --git a/tests/fixtures/alpha2-courier-export.json b/tests/fixtures/alpha2-courier-export.json
new file mode 100644
index 0000000000000000000000000000000000000000..771d904430b6f2be35506db4f8151636a24a93b6
--- /dev/null
+++ b/tests/fixtures/alpha2-courier-export.json
@@ -0,0 +1,394 @@
+{
+  "format": "briarwatch-public-save",
+  "formatVersion": 1,
+  "build": {
+    "version": "4.1.0-alpha.2"
+  },
+  "savedAt": "2026-09-11T05:03:39.618Z",
+  "state": {
+    "schemaVersion": 4,
+    "campaignId": "bell-beneath-briarwatch",
+    "createdAt": "2026-09-11T05:03:39.568Z",
+    "updatedAt": "2026-09-11T05:03:39.614Z",
+    "turnCount": 2,
+    "difficulty": "standard",
+    "player": {
+      "id": "player",
+      "kind": "player",
+      "name": "Migration Test Hero",
+      "classId": "fighter",
+      "className": "Fighter",
+      "backgroundId": "outlander",
+      "backgroundName": "Outlander",
+      "level": 1,
+      "xp": 40,
+      "abilities": {
+        "str": 15,
+        "dex": 12,
+        "con": 14,
+        "int": 8,
+        "wis": 13,
+        "cha": 10
+      },
+      "skills": [
+        "athletics",
+        "perception",
+        "survival",
+        "intimidation"
+      ],
+      "expertise": [],
+      "savingThrows": [
+        "str",
+        "con"
+      ],
+      "maxHp": 12,
+      "hp": 12,
+      "temporaryHp": 0,
+      "ac": 16,
+      "speed": 30,
+      "inventory": [
+        {
+          "itemId": "iron-longsword",
+          "quantity": 1
+        },
+        {
+          "itemId": "steel-shield",
+          "quantity": 3
+        },
+        {
